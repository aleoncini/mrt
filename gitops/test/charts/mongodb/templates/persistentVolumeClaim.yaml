apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb
  namespace: {{.Values.global.namespace}}
  labels:
    app: mongodb
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{.Values.pvc.storage}}