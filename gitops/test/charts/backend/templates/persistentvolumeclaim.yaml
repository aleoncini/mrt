apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{.Values.name}}
  namespace: {{.Values.global.namespace}}
  labels:
    app: {{.Values.name}}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{.Values.pvc.storage}}