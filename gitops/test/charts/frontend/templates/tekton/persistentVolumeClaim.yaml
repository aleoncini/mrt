apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{.Values.name}}-build
  namespace: {{.Values.global.namespace}}
spec:
  resources:
    requests:
      storage: {{.Values.build.storage}}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
